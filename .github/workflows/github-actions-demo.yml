name: Github actions demo
on: [push]
jobs:
  Explore-Github-actions:
    runs-on: self-hosted
    steps:
      - run: echo "hello"
      - name: list
        run: |
          echo $PATH
          ls ${{ github.workspace }}